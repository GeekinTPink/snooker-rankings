## 长期记忆（跨会话保留）

这份文件用于沉淀“长期稳定”的信息（不要写一次性的临时状态）。

### 项目概览
- **项目**：`snooker-rankings`
- **技术栈**：Next.js 14（App Router）、TypeScript、Tailwind
- **用途**：展示斯诺克世界排名（Top 128）

### 约定（重要）
- **输出语言**：默认中文（简体）
- **执行策略**：用户允许时可自动执行命令；用户要求“只检查不修改”时严格不改文件
- **记忆写入**：短期过程写 `memory/YYYY-MM-DD.md`；沉淀结论/约定写本文件

### 常见工程卫生
- 运行日志与生成物不应纳入版本库：例如 `logs/*.log`、`src/data/backups/`

