- Replaced placeholder scraper with Wikipedia API fetch (`scripts/scrape-rankings.py`): prefers prize-money table when present, else final seeding column; User-Agent must not be generic “bot” (403 from Wikipedia).
- UI: `SiteHeader`, home mobile card list + desktop table, pricing page restyled to snooker palette; `rankings-data.json` refreshed (126 players from 2024–25 seeding snapshot).
